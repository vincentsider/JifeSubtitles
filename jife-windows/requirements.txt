# JIFE Windows - Python Dependencies
# These are installed in the Docker image during build

# Core ASR
faster-whisper>=1.0.0

# Option B: SeamlessM4T v2 (speech-to-text translation)
transformers>=4.36.0
sentencepiece>=0.1.99

# Web server
flask>=3.0.0
flask-socketio>=5.3.0

# Audio
sounddevice>=0.4.6
scipy>=1.11.0

# Option E: Audio Preprocessing
noisereduce>=3.0.0
# demucs  # Optional: Uncomment for vocal isolation (adds ~1GB, more latency)

# Utils
numpy>=1.24.0
